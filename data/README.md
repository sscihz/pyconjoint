# Data fixtures

- `immigrationconjoint.csv` is exported from the CRAN `cjoint` package (version 2.1.1) dataset `immigrationconjoint`.
- Source repo: https://github.com/cran/cjoint
- Exported on 2026-02-05 from the `.rda` file using base R `write.csv`.

This dataset is used for PyJoint regression and cross-check tests.
